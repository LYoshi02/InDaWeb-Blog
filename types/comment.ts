export type SentComment = {
  comment: string;
  name: string;
  email: string;
  slug: string;
};

export type FetchedComment = {
  comment: string;
  name: string;
  id: string;
  createdAt: string;
};
